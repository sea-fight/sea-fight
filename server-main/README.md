FastAPI application that use Alembic, SQLAlchemy (PostgreSQL), Redis and Docker + Docker-compose

Migrate and run dev:
```bash
sudo ./migrate.sh
```

Run dev:
```bash
sudo ./dev.sh
```

Alembic Revision:
```bash
python revision.py
```
